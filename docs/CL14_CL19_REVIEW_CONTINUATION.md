# CL20-CL24: usable applications alongside scoped repairs

Satoshi implements; Musashi reviews. This continues the accepted interface,
optional integration design and existing budgets, not a new framework design.

Full independent review, unchanged CPU reproducer, results and executable order
details are in predictor:
`docs/audits/work_plan/MUSASHI_CL14_CL19_REVIEW_2026_09_24.md` and
`docs/audits/evidence/CL14_CL19_MUSASHI_20260924/`.

Reviewed: news-signal317dd35, feature-engd081d0f, M5PHET3f4a833.
Clean installed question path with explicit NON_MODEL_FIXTURE and fresh-process
recovery is accepted. Retained real-Laya native parity is not withdrawn. Recorded
collection independently reproduces 11 events plus one revision and no additions
on a second pass. This is not prospective capture or financial quality evidence.

## Required parallel delivery

- CL20: news-signal queue must not re-sign corrupt content on retry, acknowledge
  a failed exclusive write, reuse another key's record or merge source identifiers.
  Repeated identical shadow corruption must recover durably or explicitly refuse.
- CL21: feature-eng must distinguish source publication order, receipt eligibility
  and original/revision lineage; unknown publication is never receipt by default.
- **CL22: deliver the already assigned CL17 natural-language forecast slice.**
  Start alongside repairs. Data/context plus user question must resolve to a
  validated target/horizon/schema, execute an existing native trained DEV engine
  in predictor/prediction_provider, and return structured predictions with identity
  and scale. Independent native call on identical inputs is the numerical reference.
  Test paraphrases, ambiguity, unsupported horizons and transformations separately.
  No fabricated uncertainty, ideal oracle, mock prediction, new generic model or
  LLM-generated forecast may satisfy the native-engine acceptance. A bounded grammar
  is acceptable only with its restricted supported language explicitly documented.
  No heldout access, retraining or wholesale historical inference is needed.
- CL23: recorded source -> queue -> installed provider -> persisted result ->
  acknowledgement bound to the right evaluation, including crash/retry. Fixture
  integration and real-model evidence remain distinct. Prepare broker interfaces
  and quality annotation protocol independently of external access; actual capture
  and demo/paper canaries retain entitlement and risk prerequisites. No real capital.
- CL24: integrated return for every lane, including CL22 command and native parity
  or the concrete missing artifact, not just green validation tests. An unwritten
  adapter is not an owner permission dependency. Optional gov/DOIN remain optional.

Use disjoint worker/subagent worktrees where available and one integrator. Check
leases and residual allocations before any existing authorized GPU acceptance;
external5090 first. No new training allocation, duplicate jobs or global hold.
