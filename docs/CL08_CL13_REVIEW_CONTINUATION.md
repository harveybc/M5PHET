# User questions work in the pilot; deliver the installed application

Musashi review, 2026-09-24: M5PHET `3f4a833`, news-signal `e40098b`,
feature-eng `1a0402e`, predictor `1e9d994f`. Satoshi implements.

The two user-authored choice questions independently retain 12/12 exact native
SDK parity each. NL01 is now implemented, not merely specified. This is fidelity,
not an evaluation of the correctness of the two new tasks.

The declared application dependency still resolves to `e752ed1`, which rejects
the new task kinds before inference. Publish the compatible runtime and verify
successful installed ask behavior, not only imports or no-weights refusals.

Next delivery runs in parallel:

- CL14-15: installable question route, exact token-limit checks and store identity
  bound to its requested key/content. A self-valid record for another question
  cannot be returned as the requested one. Preserve original PRE cases.
- CL16: calendar surprise must distinguish publication from local receipt;
  UNKNOWN availability must be enforced by consumption, and reads must not mutate
  retained history under an unchanged vintage digest. No real-data claim from fixtures.
- CL17: implement the second natural-language slice with predictor's native
  forecasting engine and prediction_provider's serving responsibility. Compare
  a bounded existing DEV checkpoint batch, same tensors and outputs; no new model
  training or reserved-test access for an interface test. Reuse configured
  interpreter access, with validated schema/task selection and explicit refusals.
- CL18-19: collector code, labelled relevance evaluation design and demo/paper
  adapters continue; actual feed/account access has its own prerequisites. Keep
  hierarchy/causal/policy and optional DOIN integration in the consolidated status.

Do not stop all independent development for repair review. Use disjoint worktrees
and available subagents; Satoshi integrates. External 5090 preferred after fresh
admission for eligible model work, within existing remaining allocations. No real
capital, duplicate campaign, budget reset or implicit new training authorization.

Full findings, executable probes and CL14-CL19 acceptance criteria are published
in predictor on `musashi/news-live-rp144-rp151-20260924`:
`docs/audits/work_plan/MUSASHI_CL08_CL13_REVIEW_2026_09_24.md` and
`docs/audits/evidence/CL08_CL13_MUSASHI_20260924/`.
This review used CPU fixtures, clean dependency resolution and retained JSON,
not fresh GPU inference, feed or broker calls.
