# Real provider delivered; question-first application next

2026-09-24 independent review of M5PHET `e752ed1`, news-signal `bb281cc`
and predictor `8d9eba36`. Implementation: Satoshi; review: Musashi.

Retained real-weight outputs agree exactly for 12 distinct inputs, also after
process restart. This accepts the first fixed-task integration, not a generic
natural-language interface, production installation or trading accuracy.
The 13 author-labelled smoke rows score macro-F1 0.333333; unrelated recall 1/5.

## Immediate delivery

1. Repair news-signal packaging (its pinned M5PHET lacks `runtime`), store root
   containment, evaluation identity and integrity-before-reuse. A valid later
   result must not disappear behind a previously stored refusal or different task.
2. Deliver NL01: actual user question and criteria to Laya, retaining the existing
   relevance task as a preset. Validate schema and encoded budgets, bind question
   and settings identities, compare with direct SDK using identical arguments.
   Static task names do not satisfy the approved user experience.
3. In parallel implement NL02 onward with the existing forecasting engine and
   configured interpreter access. Native Laya classification does not need an
   additional interpreter; other engines receive a validated task, not arbitrary
   generated code. No new LLM training or separate plugin framework.
4. Diagnose current classification errors from actual serialized/tokenized inputs
   before prompt tuning. Separate duplication/revision tests from held-out quality
   data. Preserve direct parity and report per-class quality plus majority baseline.
5. Keep collector, calendar, native domain adapters and demo/paper interfaces
   moving in disjoint CPU lanes. External 5090 first for admitted eligible work;
   no duplicate experiments, new budget reset or live-capital execution.

Top-down acceptance tests and bottom-up implementation remain mandatory. Reuse
the existing NL01-NL08, CL01-CL07 and integration specifications, not another
abstract design phase. Local evidence is default; stack and DOIN stay optional.
RAG is optional: documentation may inform interpretation, while separately
declared point-in-time market context can inform answers through model inputs.
Neither retrieval nor an interpreter grants execution authority.

The executable PRE, full review and CL08-CL13 orders are in predictor:
`docs/audits/evidence/CL_FIRST_MUSASHI_20260924/` and
`docs/audits/work_plan/MUSASHI_CL_FIRST_REVIEW_2026_09_24.md`, published on
`musashi/news-live-rp144-rp151-20260924`.
No real weights, services or brokers were exercised by this CPU review.
